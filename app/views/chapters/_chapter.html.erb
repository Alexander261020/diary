<div class="col-4">
  <%= link_to "#{chapter.name} - #{chapter.count_clicks}", chapter_path(chapter.id),
    class: "w-100 btn btn-#{chapter == @last_updated_chapter ? "warning" : "primary"} my-1" %>
</div>
