<%= form_with(model: cargo_order) do |form| %>
  <% if cargo_order.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cargo_order.errors.count, "error") %> prohibited this cargo_order from being saved:</h2>

      <ul>
        <% cargo_order.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.hidden_field :status %>
  <div class="row">
    <div class="position-relative col-4">
      <%= text_field_tag 'customer_id', nil %>
      <div class="position-absolute border border-dark bg-light p-1" id="customers_show"
        style="display: none;"></div>
    </div>

    <div class="position-relative col-4">
      <%= text_field_tag 'carrier_id', nil %>
      <div class="position-absolute border border-dark bg-light p-1" id="carriers_show"
        style="display: none;"></div>
    </div>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>
