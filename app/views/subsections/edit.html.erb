<%= content_tag :div, (content_tag :h1, "Update \"#{@subsection.name}\""), class: 'col-6 text-center d-inline-block' %>

<%= render 'form' %>
<%= render 'blocks/form' %>

<div>
  <%= render partial: 'blocks/block_edit', collection: @subsection.blocks.order(:number_line) %>
</div>
